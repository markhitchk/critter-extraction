# Account System

Account IDs, XML imports and exports, overwrite confirmation, legacy migration, localStorage keys, IndexedDB names, stash data, loadouts, Petal balances, and career records are compatibility-sensitive. Repository organization must not rename save fields or storage keys.

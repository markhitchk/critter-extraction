# Portable Build

The canonical portable document is `portable/START_HERE.html`. Root `START_HERE.html` is generated by `node tools/build-portable.mjs` for backwards compatibility. Both must remain usable from `file://` URLs.

import fs from 'node:fs';import path from 'node:path';import{spawnSync}from'node:child_process';const roots=['core','js','assets','tools'];const files=[];function walk(d){if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=path.join(d,e.name);if(e.isDirectory())walk(f);else if(/\.(?:js|mjs)$/.test(e.name))files.push(f)}}roots.forEach(walk);let bad=0;for(const f of files){const r=spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status){bad++;console.error(r.stderr||r.stdout)}}if(bad)process.exit(1);console.log('JavaScript syntax OK:',files.length,'files');

# Stable Error Codes

Codes use `CE-<SYSTEM>-<TYPE>-<NUMBER>`. The stable code identifies a category; `eventId` identifies one occurrence. Core categories are defined in `core/error-system/error-codes.js`.

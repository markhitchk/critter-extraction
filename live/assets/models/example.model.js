/* Example only. The file test does not count this as a gameplay model. */
window.CritterStandaloneModels?.register?.({
  category:'example',
  id:'example',
  sourceFile:'assets/models/example.model.js',
  recipe:{description:'Reference registration contract for standalone model files.'}
});

(() => {
  'use strict';

  // This ID must match the generated header inside core/game/game-runtime.js.
  // Startup dependencies are loaded by game-loader.js after the boot/error
  // systems are active, so failures can always open the real error screen.
  window.CritterBuildInfo = Object.freeze({
    buildId: '3391f2959d123859',
    channel: 'github-pages',
    generatedAt: '2026-08-04T01:17:41.349Z'
  });
})();
